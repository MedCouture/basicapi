<div class="container">
    <div class="row">
        <div class="col-lg-6 jumbotron">Name: {{name}}</div>
        <div class="col-lg-6 jumbotron">Age: {{age}}</div>
    </div>
    <div class="row mt-5">
        <input type="submit" id="submit">
    </div>
</div>